import { Check, Sparkles } from 'lucide-react'
import { Link } from 'react-router-dom'
import { addons, cakeFlavours, packages } from '../data/packages'
export default function Packages(){return <main className="container pt-36 pb-24"><p className="eyebrow">House of Surprise</p><h1 className="display mt-3 text-6xl sm:text-8xl">Packages for<br/><span className="script normal-case">your moment.</span></h1><p className="mt-6 max-w-xl leading-7 text-mist">Everything you need for a beautiful private celebration. Choose a starting point, then make it completely yours.</p><div className="mt-14 grid gap-5 lg:grid-cols-3">{packages.map(item=><article key={item.name} className={`relative rounded-3xl border p-7 ${item.featured?'border-gold bg-[#eee9df]':'border-black/10 bg-card'}`}>{item.featured&&<p className="eyebrow mb-5 flex items-center gap-2"><Sparkles size={13}/> Premium experience</p>}<p className="font-numbers text-xl font-semibold">{item.name}</p><p className="mt-3 font-numbers text-4xl">₹{item.price.toLocaleString('en-IN')}</p><p className="mt-2 text-sm text-mist">{item.duration} · {item.guests}</p><div className="my-7 h-px bg-black/10"/><ul className="grid gap-3 text-sm">{item.inclusions.map(inclusion=><li className="flex gap-2" key={inclusion}><Check className="mt-0.5 size-4 shrink-0 text-gold"/>{inclusion}</li>)}</ul><Link to="/book" className="mt-8 block rounded-full bg-[#29251f] py-3 text-center text-sm font-bold text-white">Choose this package</Link></article>)}</div><section className="mt-16 grid gap-10 border-t border-black/10 pt-12 lg:grid-cols-2"><div><p className="eyebrow">Add-ons</p><h2 className="display mt-2 text-4xl">Make it <span className="script normal-case">more yours.</span></h2><div className="mt-7 grid gap-2 sm:grid-cols-2">{addons.map(item=><div className="rounded-xl border border-black/10 bg-card p-4 text-sm" key={item}>{item}</div>)}</div></div><div><p className="eyebrow">Cake flavours</p><h2 className="display mt-2 text-4xl">A sweeter <span className="script normal-case">reveal.</span></h2><div className="mt-7 grid gap-4">{cakeFlavours.map(group=><div className="rounded-2xl border border-black/10 bg-card p-5" key={group.title}><p className="font-semibold">{group.title} <span className="font-normal text-mist">· ₹{group.price}</span></p><p className="mt-2 text-sm leading-6 text-mist">{group.flavours.join(' · ')}</p></div>)}</div></div></section><section className="mt-16 rounded-3xl bg-[#29251f] p-7 text-white sm:p-10"><p className="eyebrow text-[#d8b978]">Booking notes</p><div className="mt-5 grid gap-4 text-sm leading-6 text-neutral-300 sm:grid-cols-3"><p>Advance payments are non-refundable.</p><p>Outside cakes are not permitted. Bringing your own cake changes slot charges.</p><p>Additional members ₹200 each. ₹500 midnight surprise charge applies after 10 PM.</p></div></section></main>}
