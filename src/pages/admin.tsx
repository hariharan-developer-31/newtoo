import { CalendarCheck, IndianRupee, MapPin, Plus, Users } from 'lucide-react'
import { useState } from 'react'
import type { FormEvent } from 'react'
import { toast } from 'sonner'
import { supabase } from '../lib/supabase'
export default function Admin(){const [cities,setCities]=useState(['Chennai','Pondicherry']);const [city,setCity]=useState('');const addCity=async(event:FormEvent)=>{event.preventDefault();const name=city.trim();if(!name)return;const slug=name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'');if(supabase){const {error}=await supabase.from('cities').insert({name,slug});if(error){toast.error(error.message);return}}setCities(current=>current.includes(name)?current:[...current,name]);setCity('');toast.success(`${name} is now available for booking.`)};return <main className="min-h-screen bg-ink p-6 pt-28 sm:p-10 sm:pt-28"><div className="mx-auto max-w-7xl"><p className="eyebrow">Operations console</p><h1 className="display mt-2 text-5xl">Good evening, <span className="script normal-case">team.</span></h1><div className="mt-9 grid gap-4 sm:grid-cols-3">{[['Today’s bookings','18',CalendarCheck],['Today’s revenue','₹4.86L',IndianRupee],['Guests this month','1,248',Users]].map(([label,value,Icon])=>{const I=Icon as typeof Users;return <div className="rounded-3xl border border-black/10 bg-card p-6" key={label as string}><I className="text-gold"/><p className="mt-8 text-sm text-mist">{label as string}</p><p className="mt-1 font-numbers text-3xl">{value as string}</p></div>})}</div><div className="mt-6 grid gap-6 lg:grid-cols-[1.3fr_.7fr]"><div className="rounded-3xl border border-black/10 bg-card p-6"><p className="eyebrow">Today’s events</p><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[600px] text-left text-sm"><thead className="text-xs text-mist"><tr><th className="pb-4">Guest</th><th>Experience</th><th>Time</th><th>Hall</th><th>Status</th></tr></thead><tbody>{['Manya Kapoor','Aditya Rao','Ishita Mehra'].map((guest,i)=><tr className="border-t border-black/10" key={guest}><td className="py-4">{guest}</td><td>{['Birthday','Proposal','Anniversary'][i]}</td><td>{['2:30 PM','6:30 PM','9:30 PM'][i]}</td><td>The Glass Garden</td><td><span className="rounded-full bg-success/15 px-3 py-1 text-xs text-success">Confirmed</span></td></tr>)}</tbody></table></div></div><section className="rounded-3xl border border-black/10 bg-card p-6"><div className="flex items-center justify-between"><div><p className="eyebrow">Location management</p><h2 className="mt-2 font-numbers text-xl">Booking cities</h2></div><MapPin className="text-gold"/></div><div className="mt-6 grid gap-2">{cities.map(name=><div className="flex items-center justify-between rounded-xl bg-[#f7f5f1] px-4 py-3 text-sm" key={name}><span>{name}</span><span className="text-xs text-success">Live</span></div>)}</div><form className="mt-5 flex gap-2" onSubmit={addCity}><label className="sr-only" htmlFor="city">City name</label><input id="city" value={city} onChange={e=>setCity(e.target.value)} className="min-w-0 flex-1 rounded-xl border border-black/10 px-3 text-sm outline-none focus:border-gold" placeholder="Add a city"/><button className="grid size-10 place-items-center rounded-xl bg-[#29251f] text-white" aria-label="Add city"><Plus size={18}/></button></form><p className="mt-3 text-xs leading-5 text-mist">Only cities configured here are shown in customer booking.</p></section></div></div></main>}
